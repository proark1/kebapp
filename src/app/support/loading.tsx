import { LoadingSkeleton } from "@/components/route-feedback";

export default function SupportLoading() {
  return (
    <LoadingSkeleton label="Supportbereich wird geladen" variant="admin" />
  );
}
